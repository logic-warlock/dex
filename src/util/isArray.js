/**
 * For telling if an object is an Array
 *
 * @param {any} element - Object in question
 *
 * @returns {boolean} Whether the element is an array or not
 */
export const isArray = (element) => Array.isArray(element)
